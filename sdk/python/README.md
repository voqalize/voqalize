# Python brain SDK — *(landing zone)*

The pipecat-free Python SDK for writing a Voqalize brain (`voqalcloud.sdk.Brain`)
moves in here from the source tree. Depends on the generated stubs from
[`../../proto`](../../proto). Part of the repo's `uv` workspace; the
[`demos/backend`](../../demos/backend) apps depend on it by path.

> Not moved in yet — see `docs/oss-repo-and-demos-plan.md` in the private planning
> repo, build order step 1.
