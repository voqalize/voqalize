# React client SDK — *(landing zone)*

`@voqalize/client-react` (today `@voqalcloud/client-react`) moves in here: the
browser WebRTC transport, one-call session bootstrap, and the `<VoqalAgent/>`
component / `useVoqalSession` hook. Part of the repo's `pnpm` workspace; the
[`demos/frontend`](../../demos/frontend) UIs depend on it by path.

> Not moved in yet — build order step 1. Namespace `@voqalcloud` → `@voqalize`
> is part of the coordinated rename.
