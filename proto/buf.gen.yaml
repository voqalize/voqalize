version: v2
plugins:
  # Pinned to v32.0 because pygato/agent-sdk are on protobuf-runtime 6.x.
  # Bump in lockstep with the runtime version in both packages' pyproject.toml.
  - remote: buf.build/protocolbuffers/python:v32.0
    out: gen/python
clean: true
