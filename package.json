{
  "name": "voqalize",
  "version": "0.0.0",
  "private": true,
  "description": "Voqalize public developer monorepo — root of the pnpm workspace (docs site; the demo UIs build standalone).",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
