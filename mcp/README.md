# Voqalize MCP server + skill — *(landing zone)*

`voqalize-mcp` (the stdio MCP server that manages agents over the control-plane
API) and the Claude Code skill move in here. Part of the repo's `uv` workspace.

> Not moved in yet — build order step 1.
